package org.java.coding.practices;

import java.util.Arrays;
import java.util.Calendar;
import java.util.Date;
import java.util.stream.Stream;

public class FlatMapStringArray {

	public static void main(String[] args) {
		
		/*String [][]digits = new String[][] {{"1","2"},{"3","4"},{"5","6"}};
		
		Stream<String[]> stream = Arrays.stream(digits);
		stream.flatMap(item -> Arrays.stream(item)).forEach(System.out::print);
		
		System.out.println();
		System.out.println("@@@@@@@@@");
		
		Stream<String[]> stream1 = Arrays.stream(digits);
		stream1.flatMap(item -> Arrays.stream(item)).filter(item -> "2".equals(item)).forEach(System.out::println);
		
		System.out.println("@@@@@@@@@");
		Date date = new Date();
		System.out.println(date.getTime());
		 
		long timeInMillis = Calendar.getInstance().getTimeInMillis();
		System.out.println(timeInMillis);*/
		
		try {
			System.exit(-1);
		}catch(Exception e) {
			
		} finally {
			System.out.println("Dhiren");
		}

	}

}
